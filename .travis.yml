os: 
  - ubuntu: 16.04
  
sudo: required
dist: trusty
language: generic
compiler:
  - clang
  - gcc
before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq libtinyxml-dev
  - sudo apt-get install -y python
matrix:
  allow_failures:
    - compiler: clang
